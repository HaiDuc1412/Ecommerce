# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecommerce_db
DB_USERNAME=postgres
DB_PASSWORD=root

# Server Configuration
SERVER_PORT=8080

# Application Configuration
APP_BASE_URL=http://localhost:8080

# JWT Configuration
# CRITICAL: Generate a strong secret key for production
# Use: openssl rand -base64 64
# NEVER use the example below in production!
JWT_SECRET=CHANGE_THIS_TO_A_VERY_LONG_RANDOM_STRING_FOR_PRODUCTION_USE_AT_LEAST_256_BITS
JWT_EXPIRATION=86400000

# Initial Admin & Customer Passwords
# CRITICAL: Change these immediately after first login!
# These are only for initial setup
ADMIN_PASSWORD=ChangeMe123!@#
CUSTOMER_PASSWORD=ChangeMe456!@#

# Email Configuration (SMTP)
# For Gmail, enable "App Passwords" at: https://myaccount.google.com/apppasswords
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-16-character-app-password
EMAIL_FROM=noreply@hunghypebeast.com
EMAIL_FROM_NAME=Hung Hypebeast Store

# For other email providers:
# - Outlook/Hotmail: smtp-mail.outlook.com:587
# - Yahoo: smtp.mail.yahoo.com:587
# - Custom SMTP: your-smtp-server.com:587

# Logging Level
LOG_LEVEL=INFO

# Docker Compose specific (used in docker-compose.yml)
# These can be overridden when running with Docker
# POSTGRES_DB=ecommerce_db
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=root
